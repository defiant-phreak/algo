#include <bits/stdc++.h>
using namespace std;

using ll=int64_t;
using ull=uint64_t;

#define mp make_pair
#define mt make_tuple
#define eb emplace_back
#define pb pop_back
#define fi first
#define se second
#define beg begin
#define all(x) (x).begin(),(x).end()

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(0);
	#ifdef LOCAL
		auto tic=std::chrono::high_resolution_clock::now();
	#endif
	
	#ifdef LOCAL
		auto toc=std::chrono::high_resolution_clock::now();
		cerr << "Time taken: " << std::chrono::duration_cast<std::chrono::milliseconds>(toc - tic).count() << " ms\n";
	#endif
	return 0;
}
